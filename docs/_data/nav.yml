# Sidebar navigation, rendered by _layouts/default.html.
#
# `path` is site-root-relative and does NOT carry the baseurl -- the layout prepends
# {{ site.baseurl }}. It is also what page.url is compared against to mark the current page, so the
# two have to stay in the same form: a docs/FOO.md page is built to /FOO.html, because no permalink
# style is configured in _config.yml. If a permalink style is ever added, every path here changes.
#
# `label` is the short nav name and follows the filename. `title` is the document's own H1 verbatim,
# which is not always the same thing -- ASVS-ASSESSMENT.md never says ASVS in its heading -- and is
# used as the link's title attribute.
#
# An entry with `url` instead of `path` is an absolute link off the site. Those exist because
# INSTALL.md and the scripts live at the repository root, outside the Jekyll source, so the site
# cannot serve them at all.
#
# A page in a SUBDIRECTORY follows the same rule: docs/standards/FOO.md is built to
# /standards/FOO.html. ONE FILENAME BREAKS THAT RULE, which is why the section index is called
# OVERVIEW.md and not README.md. jekyll-optional-front-matter refuses to convert a front-matter-less
# file whose basename is on its FILENAME_BLACKLIST (README, LICENSE, CONTRIBUTING and friends), so a
# README stays a static file; jekyll-readme-index then republishes it as the DIRECTORY INDEX with
# permalink File.dirname(url) + "/", i.e. /standards/, and the raw .md is served as well. There is no
# /standards/README.html for a nav path to point at. Both plugins are in the GitHub Pages
# always-enabled set and cannot be turned off; the two config keys that would change the behaviour,
# readme_index.with_frontmatter and readme_index.remove_originals, are unset in _config.yml. Naming
# the file OVERVIEW.md keeps it on the ordinary path, so its URL is predictable and page.url matches.

- group: Start here
  items:
    # FIRST ITEM OF THE FIRST GROUP, which is what makes this the first link in the sidebar on every
    # page: _layouts/default.html renders site.data.nav in file order, group by group, and takes the
    # first item of the first group as the first anchor in the nav element. Moving this entry down,
    # or adding a group above this one, silently demotes it -- the layout has no notion of a pinned
    # row, so position in this file is the whole of the mechanism.
    - label: The KORUS framework
      title: "The KORUS Framework: Lesson Learned the Hard Way"
      path: /KORUS.html
    - label: Overview
      title: claude-multisession
      path: /
    - label: Feed this to Claude Code
      title: Here's what to feed to Claude Code
      path: /FEED-THIS-TO-CLAUDE-CODE.html
    - label: Concepts
      title: Concepts
      path: /CONCEPTS.html
    # Installing sat in the "On GitHub" group, row 32 of 33, which filed the one page an installer
    # needs behind everything they would read afterwards. It is still an off-site `url` entry -- the
    # file is at the repository root, outside the Jekyll source, so the site cannot serve it -- but a
    # reader looking for how to install now finds it where they look. The label says the task rather
    # than the filename.
    - label: Installing
      title: "INSTALL.md: four installers, four scopes, and how to prove each is live"
      url: https://claude-multisession.pages.dev/INSTALL.md

- group: Running sessions
  items:
    - label: Running multiple sessions
      title: "Running multiple sessions"
      path: /RUNNING-MULTIPLE-SESSIONS.html
    - label: Desktop accounts
      title: "Several desktop instances, one per Claude account"
      path: /DESKTOP-ACCOUNTS.html
    - label: Worktrees
      title: Worktrees
      path: /WORKTREES.html
    - label: Coordination
      title: Coordination
      path: /COORDINATION.html
    - label: Steering
      title: Steering a running session
      path: /STEERING.html
    - label: Session mail
      title: "The async lane: reaching sessions the realtime channel cannot"
      path: /SESSION-MAIL.html
    - label: Sequence allocation
      title: Sequence allocation
      path: /SEQUENCE-ALLOC.html
    - label: PRs and merges
      title: "Landing work: PRs and merges"
      path: /PR-AND-MERGE.html
    - label: Pruning
      title: Pruning worktrees
      path: /PRUNING.html



- group: Guardrails
  items:
    - label: Hooks
      title: Hooks
      path: /HOOKS.html
    - label: Leak gate
      title: The leak gate
      path: /LEAK-GATE.html
    - label: Usage awareness
      title: "Usage awareness: knowing when to stop, without lying about it"
      path: /USAGE-AWARENESS.html

- group: In practice
  items:
    - label: Tips and tricks
      title: Tips and tricks
      path: /TIPS-AND-TRICKS.html
    - label: CI for leaders
      title: "CI for leaders: what done means when the author cannot vouch for the change"
      path: /CI-FOR-LEADERS.html
    - label: Drift audit case study
      title: "Case study: auditing a multi-session estate as one system"
      path: /CASE-STUDY-drift-audit.html
    - label: Correction chain case study
      title: "Case study: a finding that took four passes and two sessions"
      path: /CASE-STUDY-correction-chain.html

# Evaluations of third-party frameworks, one page each. The naming follows the CASE-STUDY-* series
# already in "In practice": a shared prefix, then a kebab-case name, so a second evaluation is
# FRAMEWORK-<name>.md and sorts beside this one in docs/. Each page is scoped to what a reader
# running CONCURRENT SESSIONS needs from that framework -- the general tool review is upstream's
# job, and duplicating it here would be the drift HS-3 exists to prevent.
- group: Other frameworks
  items:
    - label: Spec Kit
      title: "Spec Kit 0.16.4, evaluated"
      path: /FRAMEWORK-spec-kit.html

- group: Contributing
  items:
    - label: House style
      title: "House style: what a page here has to do"
      path: /HOUSE-STYLE.html

- group: The standards
  items:
    # These documents lived here until the split and are now their own repository. The group is one
    # row on purpose: this site routes to them, it does not mirror them, and a second copy of the
    # index is the drift both projects are about.
    - label: Security standards
      title: Security standards for teams building with AI coding assistants
      url: https://secure-development-standards.pages.dev/

- group: On GitHub
  items:
    - label: CLAUDE.md template
      title: A working agreement to copy into your own repository
      url: https://claude-multisession.pages.dev/CLAUDE.md.template
    - label: Source repository
      title: claude-multisession on GitHub
      url: https://github.com/wshallwshall/claude-multisession
